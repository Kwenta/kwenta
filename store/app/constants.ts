import { getAppKey } from '../utils';

export const priceCurrencyStateKey = getAppKey('priceCurrency');
export const languageStateKey = getAppKey('language');
export const chartPeriodStateKey = getAppKey('chartPeriod');
