"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
const body_1 = require("../segments/body");
/**
 * Writes a single file which contains types and async functions for every queryable entity.
 */
function plain(schema) {
    const out = [];
    out.push((0, body_1.heading)());
    for (const entity of schema.types) {
        const filterEntity = schema.types.find(e => e.name === entity.name + '_filter');
        if (!filterEntity)
            continue;
        out.push((0, body_1.types)(entity, filterEntity));
        out.push(`export const get${entity.name}ById = ${(0, body_1.singleBody)(entity)};`);
        out.push(`export const get${entity.name}s = ${(0, body_1.multiBody)(entity)};`);
    }
    return out.join('\n');
}
exports.default = plain;
