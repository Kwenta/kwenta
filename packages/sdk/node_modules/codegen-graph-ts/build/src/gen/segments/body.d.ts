import { Entity } from "../types";
/**
 * Required imports and primitive types required for other functions
 * @returns Text to be injected for the heading
 */
export declare function heading(): string;
/**
 * Generates an async function body for fetching and parsing query options
 */
export declare function multiBody(e: Entity): string;
/**
 * Generates an async function body for fetching and parsing query options
 */
export declare function singleBody(e: Entity): string;
export declare function types(entity: Entity, filterEntity: Entity): string;
