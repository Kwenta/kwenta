#!/usr/bin/env node
import { Schema } from './types';
interface PullOptions {
    url?: string;
}
interface GenOptions extends PullOptions {
    method: string;
    outdir: string;
    schema: Schema;
    js?: boolean;
}
export declare function pull(options: PullOptions): Promise<Schema>;
export declare function gen({ schema, method, js }: GenOptions): string;
export {};
